{
  "name": "foundry-template",
  "version": "1.0.0",
  "license": "MIT",
  "files": [
    "src/**/*.sol"
  ],
  "devDependencies": {
    "husky": "^8.0.3",
    "lint-staged": "^14.0.1"
  },
  "lint-staged": {
    "{src,script,test}/**/*.sol": [
      "forge fmt"
    ]
  },
  "scripts": {
    "prepare": "husky install"
  }
}